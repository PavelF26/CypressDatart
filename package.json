{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:run": "cypress run --browser chrome --headed",
    "cy:run": "cypress run --spec 'cypress/e2e/1./testDatart.cy.js/**/*.js'"

  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.6.6",
    "cypress-xpath": "^2.0.1"
  }
}
